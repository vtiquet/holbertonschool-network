# Task 1: The Infrastructure Diagram (1-what_happen_when_diagram)

This file should contain the **URL** to your hosted diagram. Below is a logic map you can use to build your visual in tools like `diagrams.net` or `Mermaid.js`.

## Mermaid Diagram Structure

```mermaid
graph TD
    User((Enter google.com)) --> DNS{DNS Lookup}
    DNS -->|Get IP| TCP[TCP 3-Way Handshake]
    TCP --> TLS[TLS 1.3 Encryption]
    TLS --> HTTP[HTTP GET Request]
    HTTP --> FW[Firewall]
    FW --> LB[Load Balancer]
    LB --> WS[Web Server]
    WS --> AS[Application Server]
    AS --> DB[(Database)]
    DB --> AS
    AS --> WS
    WS -->|HTML/CSS/JS| User

```
